/**
 * ============================================================================
 * HOOKS INDEX
 * ============================================================================
 * Central export for all custom React hooks.
 *
 * Usage:
 *   import { useTranscriptAutoPopulate } from '@hooks';
 * ============================================================================
 */

export { useTranscriptAutoPopulate } from './useTranscriptAutoPopulate';

// Default export for convenience
export { useTranscriptAutoPopulate as default } from './useTranscriptAutoPopulate';
